#include "Function.h"
#include "Queue.h"
#include "BST.h"



int main(void)
{
	TreeNode *pRoot = NULL;
	
	/*
	Queue q;

	initQueue(&q);
	enqueue(&q, 10);
	enqueue(&q, 42);
	enqueue(&q, 56);
	recursivePrint(q);
	*/
	/*
	//printf("sizeOf (Contact): %d\n", sizeof(Contact));

	Contact *pFirstContact = NULL;

	//pFirstContact = createContact();

	printf("Email: %s\n", pFirstContact->pEmail);
	*/

	return 0;
}